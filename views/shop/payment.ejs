<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secure Payment Gateway | Zorin Mobile Shop</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #004768;
            --success-color: #28a745;
            --danger-color: #dc3545;
            --warning-color: #ffc107;
            --light-color: #f8f9fa;
            --dark-color: #333;
            --border-color: #dee2e6;
            --shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .payment-container {
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
        }

        .payment-card {
            background: white;
            border-radius: 20px;
            box-shadow: var(--shadow);
            overflow: hidden;
            animation: fadeIn 0.5s ease-out;
        }

        .payment-header {
            background: linear-gradient(135deg, var(--primary-color) 0%, #006992 100%);
            color: white;
            padding: 40px 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .payment-header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 1px, transparent 1px);
            background-size: 20px 20px;
            opacity: 0.3;
            transform: rotate(30deg);
        }

        .payment-header i {
            font-size: 3.5rem;
            margin-bottom: 20px;
            display: block;
            animation: pulse 2s infinite;
        }

        .payment-header h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 10px;
            position: relative;
        }

        .payment-header p {
            font-size: 1.2rem;
            opacity: 0.9;
            position: relative;
        }

        .payment-content {
            padding: 50px 40px;
        }

        .order-info {
            background: var(--light-color);
            border-radius: 12px;
            padding: 25px;
            margin-bottom: 40px;
            border-left: 5px solid var(--primary-color);
        }

        .order-info h3 {
            color: var(--primary-color);
            margin-bottom: 15px;
            font-size: 1.3rem;
        }

        .order-id {
            display: flex;
            align-items: center;
            gap: 15px;
            background: white;
            padding: 15px;
            border-radius: 8px;
            border: 2px dashed var(--border-color);
        }

        .order-id i {
            color: var(--primary-color);
            font-size: 1.5rem;
        }

        .order-id span {
            font-family: 'Courier New', monospace;
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--dark-color);
        }

        .payment-status {
            text-align: center;
            margin-bottom: 40px;
        }

        .payment-status h3 {
            font-size: 1.4rem;
            color: var(--dark-color);
            margin-bottom: 15px;
        }

        .payment-status p {
            color: #666;
            max-width: 500px;
            margin: 0 auto 25px;
            line-height: 1.6;
        }

        .simulation-buttons {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }

        .btn {
            padding: 20px 30px;
            border-radius: 12px;
            border: none;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            text-decoration: none;
            color: white;
        }

        .btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
        }

        .btn-success {
            background: linear-gradient(135deg, var(--success-color) 0%, #218838 100%);
        }

        .btn-success:hover {
            background: linear-gradient(135deg, #218838 0%, #1e7e34 100%);
        }

        .btn-danger {
            background: linear-gradient(135deg, var(--danger-color) 0%, #c82333 100%);
        }

        .btn-danger:hover {
            background: linear-gradient(135deg, #c82333 0%, #bd2130 100%);
        }

        .btn i {
            font-size: 1.3rem;
        }

        .security-info {
            margin-top: 40px;
            padding: 25px;
            background: #fff8e1;
            border-radius: 12px;
            border-left: 5px solid var(--warning-color);
        }

        .security-header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 15px;
        }

        .security-header i {
            color: var(--warning-color);
            font-size: 1.8rem;
        }

        .security-header h4 {
            color: var(--dark-color);
            font-size: 1.2rem;
        }

        .security-info p {
            color: #666;
            font-size: 0.95rem;
            line-height: 1.5;
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.05);
            }

            100% {
                transform: scale(1);
            }
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .payment-header {
                padding: 30px 20px;
            }

            .payment-header h1 {
                font-size: 2rem;
            }

            .payment-header i {
                font-size: 2.8rem;
            }

            .payment-content {
                padding: 40px 25px;
            }

            .simulation-buttons {
                grid-template-columns: 1fr;
            }

            .btn {
                padding: 18px 25px;
            }
        }

        @media (max-width: 576px) {
            body {
                padding: 15px;
            }

            .payment-header {
                padding: 25px 15px;
            }

            .payment-header h1 {
                font-size: 1.8rem;
            }

            .payment-content {
                padding: 30px 20px;
            }

            .order-info {
                padding: 20px;
            }

            .order-id {
                flex-direction: column;
                text-align: center;
                gap: 10px;
            }

            .btn {
                flex-direction: column;
                gap: 10px;
                padding: 15px;
            }

            .btn i {
                font-size: 1.5rem;
            }
        }

        /* Loading animation for buttons */
        .btn.loading {
            position: relative;
            pointer-events: none;
            opacity: 0.8;
        }

        .btn.loading::after {
            content: '';
            position: absolute;
            width: 20px;
            height: 20px;
            border: 2px solid white;
            border-radius: 50%;
            border-top-color: transparent;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }
    </style>
</head>

<body>
    <div class="payment-container">
        <div class="payment-card">
            <div class="payment-header">
                <i class="fas fa-lock"></i>
                <h1>Secure Payment Gateway</h1>
                <p>Safe and encrypted transaction processing</p>
            </div>

            <div class="payment-content">
                <div class="order-info">
                    <h3>Order Details</h3>
                    <div class="order-id">
                        <i class="fas fa-receipt"></i>
                        <div>
                            <div style="font-size: 0.9rem; color: #666; margin-bottom: 5px;">Processing payment for:
                            </div>
                            <span>
                                <%= orderId %>
                            </span>
                        </div>
                    </div>
                </div>

                <div class="payment-status">
                    <h3>Payment Simulation</h3>
                    <p>This is a demonstration of our payment processing system. Choose an outcome to simulate the
                        payment result.</p>

                    <div class="simulation-buttons">
                        <button onclick="processPayment('success')" class="btn btn-success">
                            <i class="fas fa-check-circle"></i>
                            <span>Simulate Successful Payment</span>
                        </button>

                        <button onclick="processPayment('failure')" class="btn btn-danger">
                            <i class="fas fa-times-circle"></i>
                            <span>Simulate Failed Payment</span>
                        </button>
                    </div>
                </div>

                <div class="security-info">
                    <div class="security-header">
                        <i class="fas fa-shield-alt"></i>
                        <h4>Secure Transaction</h4>
                    </div>
                    <p>All transactions are encrypted using 256-bit SSL encryption. Your payment information is securely
                        processed and never stored on our servers.</p>
                </div>
            </div>
        </div>
    </div>

    <script>
        function processPayment(status) {
            const buttons = document.querySelectorAll('.btn');
            const orderId = '<%= orderId %>';

            // Add loading state to all buttons
            buttons.forEach(btn => {
                btn.classList.add('loading');
                btn.style.pointerEvents = 'none';
            });

            // Simulate processing delay
            setTimeout(() => {
                if (status === 'success') {
                    window.location.href = `/payment/success?orderId=${orderId}`;
                } else {
                    window.location.href = `/payment/failure?orderId=${orderId}`;
                }
            }, 1500);
        }

        // Add some interactive elements
        document.addEventListener('DOMContentLoaded', function () {
            // Animate security icon
            const lockIcon = document.querySelector('.payment-header i');
            setInterval(() => {
                lockIcon.style.animation = 'none';
                setTimeout(() => {
                    lockIcon.style.animation = 'pulse 2s infinite';
                }, 10);
            }, 4000);

            // Add keyboard shortcuts
            document.addEventListener('keydown', function (e) {
                if (e.key === '1' || e.key === 's') {
                    processPayment('success');
                } else if (e.key === '2' || e.key === 'f') {
                    processPayment('failure');
                }
            });
        });
    </script>
</body>

</html>