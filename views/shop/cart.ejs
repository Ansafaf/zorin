<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart - Zorin Mobile Shop</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f5f7fa;
            padding: 20px;
            color: #333;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .page-header {
            text-align: center;
            margin-bottom: 40px;
            padding: 30px 20px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .page-header h1 {
            font-size: 2.5rem;
            color: #2c3e50;
            margin-bottom: 10px;
        }

        .page-header p {
            color: #7f8c8d;
            font-size: 1rem;
        }

        .cart-container {
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        /* Desktop Table View */
        .cart-table {
            width: 100%;
            border-collapse: collapse;
        }

        .cart-table thead {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .cart-table th {
            padding: 18px 20px;
            text-align: left;
            font-weight: 600;
            font-size: 0.95rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .cart-table tbody tr {
            border-bottom: 1px solid #e9ecef;
            transition: background 0.3s ease;
        }

        .cart-table tbody tr:hover {
            background: #f8f9fa;
        }

        .cart-table td {
            padding: 20px;
            vertical-align: middle;
        }

        .product-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .product-img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 8px;
            border: 2px solid #e9ecef;
        }

        .product-name {
            font-weight: 600;
            color: #2c3e50;
            font-size: 1rem;
        }

        .price {
            font-size: 1.1rem;
            font-weight: 600;
            color: #667eea;
        }

        .quantity {
            display: inline-block;
            padding: 8px 16px;
            background: #f8f9fa;
            border-radius: 6px;
            font-weight: 600;
            color: #495057;
        }

        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 6px;
            font-size: 0.95rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-danger {
            background: #dc3545;
            color: white;
        }

        .btn-danger:hover {
            background: #c82333;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(220, 53, 69, 0.3);
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 14px 32px;
            font-size: 1.1rem;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
        }

        .cart-summary {
            padding: 30px;
            background: #f8f9fa;
            border-top: 3px solid #667eea;
        }

        .summary-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .summary-row h2 {
            font-size: 1.8rem;
            color: #2c3e50;
        }

        .total-price {
            font-size: 2rem;
            font-weight: 700;
            color: #667eea;
        }

        .checkout-btn-container {
            text-align: right;
        }

        /* Mobile Card View */
        .cart-items-mobile {
            display: none;
        }

        .cart-item-card {
            padding: 20px;
            border-bottom: 1px solid #e9ecef;
        }

        .cart-item-card:last-child {
            border-bottom: none;
        }

        .item-header {
            display: flex;
            gap: 15px;
            margin-bottom: 15px;
        }

        .item-details {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-bottom: 15px;
        }

        .detail-group {
            display: flex;
            flex-direction: column;
        }

        .detail-label {
            font-size: 0.85rem;
            color: #6c757d;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 5px;
        }

        .detail-value {
            font-size: 1.1rem;
            font-weight: 600;
            color: #2c3e50;
        }

        /* Empty Cart */
        .empty-cart {
            text-align: center;
            padding: 60px 20px;
        }

        .empty-cart-icon {
            font-size: 5rem;
            color: #dee2e6;
            margin-bottom: 20px;
        }

        .empty-cart h2 {
            font-size: 1.8rem;
            color: #6c757d;
            margin-bottom: 15px;
        }

        .empty-cart p {
            color: #adb5bd;
            margin-bottom: 30px;
        }

        /* Tablet styles */
        @media (max-width: 992px) {

            .cart-table th,
            .cart-table td {
                padding: 15px;
                font-size: 0.9rem;
            }

            .product-img {
                width: 60px;
                height: 60px;
            }

            .page-header h1 {
                font-size: 2rem;
            }
        }

        /* Mobile styles */
        @media (max-width: 768px) {
            body {
                padding: 10px;
            }

            .page-header {
                margin-bottom: 20px;
                padding: 20px 15px;
            }

            .page-header h1 {
                font-size: 1.8rem;
            }

            .cart-table {
                display: none;
            }

            .cart-items-mobile {
                display: block;
            }

            .cart-summary {
                padding: 20px;
            }

            .summary-row {
                flex-direction: column;
                align-items: flex-start;
                gap: 15px;
            }

            .summary-row h2 {
                font-size: 1.4rem;
            }

            .total-price {
                font-size: 1.6rem;
            }

            .checkout-btn-container {
                text-align: center;
                width: 100%;
            }

            .btn-primary {
                width: 100%;
                padding: 16px;
            }

            .product-img {
                width: 70px;
                height: 70px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="page-header">
            <h1>Shopping Cart</h1>
            <p>Review your items before checkout</p>
        </div>

        <% if (cart && cart.items.length> 0) { %>
            <div class="cart-container">
                <!-- Desktop Table View -->
                <table class="cart-table">
                    <thead>
                        <tr>
                            <th>Product</th>
                            <th>Price</th>
                            <th>Quantity</th>
                            <th>Total</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% cart.items.forEach(item=> { %>
                            <tr>
                                <td>
                                    <div class="product-info">
                                        <% if(item.img) { %>
                                            <img src="/uploads/<%= item.img %>" alt="<%= item.name %>"
                                                class="product-img">
                                            <% } else { %>
                                                <img src="https://via.placeholder.com/80" alt="<%= item.name %>"
                                                    class="product-img">
                                                <% } %>
                                                    <span class="product-name">
                                                        <%= item.name %>
                                                    </span>
                                    </div>
                                </td>
                                <td class="price">₹<%= item.price %>
                                </td>
                                <td><span class="quantity">
                                        <%= item.quantity %>
                                    </span></td>
                                <td class="price">₹<%= item.price * item.quantity %>
                                </td>
                                <td>
                                    <form action="/cart/delete-item" method="POST" style="display: inline;">
                                        <input type="hidden" name="productId" value="<%= item.productId %>">
                                        <button type="submit" class="btn btn-danger">Remove</button>
                                    </form>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>

                <!-- Mobile Card View -->
                <div class="cart-items-mobile">
                    <% cart.items.forEach(item=> { %>
                        <div class="cart-item-card">
                            <div class="item-header">
                                <% if(item.img) { %>
                                    <img src="/uploads/<%= item.img %>" alt="<%= item.name %>" class="product-img">
                                    <% } else { %>
                                        <img src="https://via.placeholder.com/80" alt="<%= item.name %>"
                                            class="product-img">
                                        <% } %>
                                            <div>
                                                <div class="product-name">
                                                    <%= item.name %>
                                                </div>
                                            </div>
                            </div>
                            <div class="item-details">
                                <div class="detail-group">
                                    <span class="detail-label">Price</span>
                                    <span class="detail-value price">₹<%= item.price %></span>
                                </div>
                                <div class="detail-group">
                                    <span class="detail-label">Quantity</span>
                                    <span class="detail-value">
                                        <%= item.quantity %>
                                    </span>
                                </div>
                                <div class="detail-group">
                                    <span class="detail-label">Total</span>
                                    <span class="detail-value price">₹<%= item.price * item.quantity %></span>
                                </div>
                            </div>
                            <form action="/cart/delete-item" method="POST">
                                <input type="hidden" name="productId" value="<%= item.productId %>">
                                <button type="submit" class="btn btn-danger" style="width: 100%;">Remove Item</button>
                            </form>
                        </div>
                        <% }) %>
                </div>

                <div class="cart-summary">
                    <div class="summary-row">
                        <h2>Total:</h2>
                        <span class="total-price">₹<%= cart.totalPrice %></span>
                    </div>
                    <div class="checkout-btn-container">
                        <a href="/checkout" class="btn btn-primary">Proceed to Checkout</a>
                    </div>
                </div>
            </div>
            <% } else { %>
                <div class="cart-container">
                    <div class="empty-cart">
                        <div class="empty-cart-icon">🛒</div>
                        <h2>Your cart is empty</h2>
                        <p>Looks like you haven't added any items yet</p>
                        <a href="/" class="btn btn-primary">Continue Shopping</a>
                    </div>
                </div>
                <% } %>
    </div>
</body>

</html>